<nav class="bg-white shadow-md rounded-b-xl mb-8">
  <div class="max-w-4xl mx-auto px-4">
    <div class="flex items-center justify-between h-16">
      <div class="flex items-center space-x-6">
        <%= link_to 'Home', root_path, class: 'text-lg font-semibold hover:text-blue-600 transition' %>
        <% if user_signed_in? %>
          <% if current_user.partner? %>
            <%= link_to 'My Services', services_path, class: 'hover:text-blue-600 transition' %>
            <%= link_to 'My Availabilities', availabilities_path, class: 'hover:text-blue-600 transition' %>
          <% elsif current_user.client? %>
            <%= link_to 'Book a Service', bookings_path, class: 'hover:text-blue-600 transition' %>
            <%= link_to 'My Subscriptions', subscriptions_path, class: 'hover:text-blue-600 transition' %>
          <% end %>
        <% end %>
      </div>
      <div class="flex items-center space-x-4">
        <% if user_signed_in? %>
          <%= link_to 'Edit Profile', edit_user_registration_path, class: 'hover:text-blue-600 transition' %>
          <%= link_to 'Logout', destroy_user_session_path, method: :delete, class: 'hover:text-red-500 transition' %>
        <% else %>
          <%= link_to 'Sign Up', new_user_registration_path, class: 'hover:text-blue-600 transition' %>
          <%= link_to 'Login', new_user_session_path, class: 'hover:text-blue-600 transition' %>
        <% end %>
      </div>
    </div>
  </div>
</nav>